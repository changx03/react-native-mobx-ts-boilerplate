{
  "name": "react-native-mobx-list",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "tsc": "tsc",
    "clean": "rimraf build",
    "build": "yarn clean && yarn tsc",
    "lint": "tslint --project tslint.json src/**/*.ts",
    "watch": "yarn build -w",
    "test": "jest"
  },
  "dependencies": {
    "mobx": "^3.3.2",
    "mobx-react": "^4.3.5",
    "react": "16.0.0",
    "react-native": "0.50.4"
  },
  "devDependencies": {
    "@types/jest": "^21.1.8",
    "@types/react": "^16.0.25",
    "@types/react-native": "^0.50.7",
    "babel-jest": "21.2.0",
    "babel-preset-react-native": "4.0.0",
    "jest": "21.2.1",
    "react-test-renderer": "16.0.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.8.0",
    "typescript": "^2.6.2"
  },
  "jest": {
    "preset": "react-native"
  }
}
